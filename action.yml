name: "Hello-World"
description: "Great someone and record time"
inputs:
  who-to-greet: # id of input
    description:'Who to greet'
    required:true
    default:'World'
outputs:
  time: description:'The time we greet'

runs: using:'node16'
  main:'dist/index.js'
